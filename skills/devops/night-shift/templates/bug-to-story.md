# Story: Fix <BUG-ID> — <short description>

As a developer, I want <what should work> so that <why it matters>.

## Tags
bugfix, <severity>, <area>

## Acceptance Criteria
- [ ] <Specific, testable fix criterion — e.g. "ForEach in X.swift:NN uses explicit id: key path">
- [ ] `xtool dev build` passes
- [ ] No new compiler warnings introduced

## Technical Context
- Bug report: docs/stories/<BUG-ID>.md (do not delete — this story references it)
- File: <path/to/file.swift:line>
- Root cause: <one-line description from bug report>
- Suggested fix: <from bug report's Fix section>

## Notes
- <Dependencies on other bug fixes, ordering constraints, or what NOT to touch>
